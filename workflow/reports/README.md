# Workflow Reports

This folder stores optional run artifacts such as:
- `bootstrap_status.json`
- per-iteration summaries
- review snapshots

These files are intentionally separate from source code so the workflow remains useful before and after local git bootstrap.

The reporter summarizes git readiness, enabled iterations, and whether bootstrap has already been completed.
